import Genything

// Introduce how we tend to use arbitrary to have instant access to arbitrary data structures for when we don't care about what the type is like (invariant testing)
// Show how to modify the base arbitrary structure when you do have limitations
