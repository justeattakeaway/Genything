/// A namespace for generated which have been liften into a `LazyGenerator` type. This has likely been done because the wrapped generator has state requirements and will behave differently than an arbitrary generator when shared or recreated.
enum DeferredGenerators {}
